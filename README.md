These are some of my tools.
Else is in rongtools\__init__.py start
Translate the rest yourself.
这是我的一个包
他有我的工具
playmove.py是可以创建一个可移动的东西（使用add_self(image=None,title="rongmove")创建）
上下左右分别是
Up(距离)
Down(距离)
Right(距离)
Left(距离)
单位是像素
windowtools.py是窗口工具，现在有incenter(窗口的w,窗口的h)和qtmainwindow(输入窗口的类名，不用加引号，在Qt源文件里用)
